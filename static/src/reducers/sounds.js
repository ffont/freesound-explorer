export const initialState = {
  byID: {},
  isFetching: false,
  error: false,
};

const sounds = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default sounds;
